{
  "git": {
    "deploymentEnabled": {
      "pipz-web": true
    }
  },
  "buildCommand": "cd ../.. && npm install && cd packages/core && npm run build && cd ../apps/web && npx next build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "cd ../.. && npm install",
  "framework": "nextjs"
}
